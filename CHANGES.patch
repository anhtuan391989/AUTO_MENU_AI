diff --git a/ui/css/style.css b/ui/css/style.css
index 8039fe3..e1a2f52 100644
--- a/ui/css/style.css
+++ b/ui/css/style.css
@@ -10,7 +10,7 @@ body { background: radial-gradient(circle at top, #0b2036, #020813 65%); font-fa
 /* =====================================
 2. HEADER & STATUS
 ===================================== */
-.header { height: 47px; display: grid; grid-template-columns: 220px 1fr 170px; align-items: center; padding: 0 11px; border: 1px solid rgba(0,180,255,.35); border-radius: 11px; background: linear-gradient(180deg,rgba(0,25,45,.55),rgba(0,0,0,.55));} /* v3.0 Mục V: thu nhỏ thêm ~10% từ 52px */
+.header { height: 47px; display: grid; grid-template-columns: 220px 1fr 170px; column-gap: 18px; align-items: center; padding: 0 11px; border: 1px solid rgba(0,180,255,.35); border-radius: 11px; background: linear-gradient(180deg,rgba(0,25,45,.55),rgba(0,0,0,.55));} /* v3.0 Mục V: thu nhỏ thêm ~10% từ 52px. MENU UI FIX Mục 3: thêm column-gap để dot AI (cuối cột 1) không dính sát NOW PLAYING (đầu cột 2) — không đổi độ rộng/vị trí từng cột */
 .logo { font-size: 18px; font-weight: 900; color: var(--cyan); letter-spacing: 1px; display: flex; align-items: center; gap: 7px; white-space: nowrap; }
 .logo-dot { flex-shrink: 0; }
 .header-center { display: flex; align-items: center; justify-content: flex-start; gap: 20px; min-width: 0; /* bắt buộc để flex item con co lại đúng, không đẩy tràn header */ }
@@ -75,14 +75,14 @@ body { background: radial-gradient(circle at top, #0b2036, #020813 65%); font-fa
 #minBtn:hover { background: #555; }
 .preset-btn:hover:not(.active) {border-color: var(--cyan); background: linear-gradient(180deg, #0d2238, #0a1826); box-shadow: 0 0 10px rgba(0, 180, 255, 0.3); transform: translateY(-2px);}
 .analyzer-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 6px; margin-bottom: 2px; }
-.analyzer-card { height: 104px; border-radius: 12px; border: 1px solid rgba(0,180,255,.25); background: linear-gradient(180deg,#081523,#05101a); display: flex; flex-direction: column; justify-content: center; align-items: center; }
-.analyzer-value { font-size: clamp(26px, 3.2vw, 38px); font-weight: 900; color: #13dfff; max-width: 100%; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; } /* clamp() thay vì hardcode — Mục X: không giảm khả năng đọc, chỉ chống tràn khi text dài */
+.analyzer-card { height: 104px; border-radius: 12px; border: 1px solid rgba(0,180,255,.25); background: linear-gradient(180deg,#081523,#05101a); display: flex; flex-direction: column; justify-content: center; align-items: center; overflow: hidden; } /* MENU UI FIX Mục 1: overflow:hidden làm lưới an toàn cuối — nội dung đã được nén vừa khít, không dựa vào việc cắt chữ */
+.analyzer-value { font-size: clamp(22px, 2.5vw, 30px); line-height: 1.1; font-weight: 900; color: #13dfff; max-width: 100%; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; } /* MENU UI FIX Mục 1: giảm từ clamp(26,3.2vw,38) — tránh tràn card 104px khi cộng đủ 4 dòng info-side */
+#modTime{ font-size: clamp(22px, 2.5vw, 30px); line-height:1.1; max-width: 100%; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;}
+#modStatus{ font-size:10px; opacity:.85; max-width: 100%; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; margin-top: 1px; line-height: 1.25;}
+#modTimeline{ font-size:9px; opacity:.65; letter-spacing:1px; line-height: 1.25;}
+.bpm-value-line{ font-size:13px; font-weight:700; color:#9fe9ff; margin-top:1px; line-height: 1.2;}
 .control-grid { display: grid; grid-template-columns: repeat(5,1fr); gap: 6px; margin-bottom: 2px; }
 .control-btn { height: 42px; border: 1px solid rgba(0,180,255,.25); border-radius: 9px; background: linear-gradient(180deg,#0a1826,#07111c); color: #13dfff; font-weight: 800; cursor: pointer; } /* v3.0 Mục VIII: giảm ~10% từ 47px */
-#modTime{ font-size: clamp(26px, 3.2vw, 40px); line-height:1.3; max-width: 100%; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;}
-#modStatus{ font-size:11px; opacity:.85; max-width: 100%; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; margin-top: 3px;} /* v3.2: nhỏ lại (13->11px), modTime vẫn là trọng tâm */
-#modTimeline{ font-size:9px; opacity:.65; letter-spacing:1px;} /* v3.2: nhỏ lại (11->9px) */
-.bpm-value-line{ font-size:15px; font-weight:700; color:#9fe9ff; margin-top:3px;} /* v3.2: +1px margin-top để căn hierarchy đẹp hơn */
 #bpmValue + .analyzer-sub { margin-top: 3px; } /* v3.2: chỉ nhắm đúng dòng "Confidence 98%" của BPM card, không đụng .analyzer-sub dùng chung (tránh nới lỏng KEY card đang cần gọn) */
 
 /* Mục XII — ACTIVE SOURCE: 1 dòng badge duy nhất, ẩn các dòng trạng thái cũ dễ gây rối
@@ -177,9 +177,14 @@ không đọc kích thước phần tử, nên đổi số đo ở đây không
 .apply-btn-mini { width: 100%; background: #00d4ff; color: #000; border: none; font-size: 9px; font-weight: bold; cursor: pointer; border-radius: 2px;}
 
 /* Tinh chỉnh để Icon và Sub nằm gọn trong info-side */
-.info-side { text-align: left; display: flex; flex-direction: column; gap: 2px;}
-.analyzer-icon { font-size: 15px; color: var(--cyan); }
-.analyzer-sub { font-size: 10px; color: #888; }
+.info-side { text-align: left; display: flex; flex-direction: column; gap: 1px;}
+.analyzer-icon { font-size: 13px; color: var(--cyan); display: flex; align-items: center; gap: 6px; line-height: 1.1; }
+.analyzer-sub { font-size: 9px; color: #888; line-height: 1.25; }
+
+/* MENU UI FIX (Claude 2, Mục 2): dot trạng thái KEY/BPM/MOD giờ nằm NGAY TRONG dòng title
+   (.analyzer-icon), luôn dính sát chữ, không còn absolute-position ở góc card (dễ trông như
+   trạng thái chung của cả card, đúng lỗi Mục 2 mô tả). Không đổi vị trí/kích thước card. */
+.inline-dot { flex-shrink: 0; margin-left: 0; } /* .analyzer-icon đã có gap:6px lo khoảng cách với chữ — không cộng dồn thêm margin */
 
 /* Tinh chỉnh lại card content */
 .card-content { display: flex; justify-content: space-between; align-items: center; width: 100%; padding: 0 10px;}
diff --git a/ui/index.html b/ui/index.html
index 0f689da..e7bbda7 100644
--- a/ui/index.html
+++ b/ui/index.html
@@ -117,10 +117,9 @@
         <!-- ANALYZER -->
         <section class="analyzer-grid">
             <div class="analyzer-card">
-                <div id="dot-key" class="status-dot offline" style="position:absolute; top:10px; right:12px;"></div>
                 <div class="card-content">
                     <div class="info-side">
-                        <div class="analyzer-icon">🎼 KEY</div>
+                        <div class="analyzer-icon">🎼 KEY<span id="dot-key" class="status-dot offline inline-dot"></span></div>
                         <div class="analyzer-title"></div>
                         <div id="currentKey" class="analyzer-value">G# Minor</div>
                         <div id="activeSourceLabel" class="analyzer-sub active-source-label">ACTIVE: AI DETECT</div>
@@ -163,18 +162,16 @@
                 </div>
             </div>
             <div class="analyzer-card">
-                <div id="dot-bpm" class="status-dot offline" style="position:absolute; top:10px; right:12px;"></div>
-                <div class="analyzer-icon">📈 BPM</div>
+                <div class="analyzer-icon">📈 BPM<span id="dot-bpm" class="status-dot offline inline-dot"></span></div>
                 <div class="analyzer-title"></div>
                 <div id="currentBpm" class="analyzer-value">128</div>
                 <div id="bpmValue" class="bpm-value-line">-- BPM</div>
                 <div class="analyzer-sub">Confidence 98%</div>
             </div>
             <div class="analyzer-card">
-                <div id="dot-mod" class="status-dot offline" style="position:absolute; top:10px; right:12px;"></div>
                 <div class="card-content">
                     <div class="info-side">
-                        <div class="analyzer-icon">🕒 MOD</div>
+                        <div class="analyzer-icon">🕒 MOD<span id="dot-mod" class="status-dot offline inline-dot"></span></div>
                         <div class="analyzer-title"></div>
                         <div id="modTime" class="analyzer-value">02:15</div>
                         <div id="modStatus" class="analyzer-sub">G#m → Am</div>
diff --git a/ui/js/renderer.js b/ui/js/renderer.js
index a8b8995..046a60e 100644
--- a/ui/js/renderer.js
+++ b/ui/js/renderer.js
@@ -634,18 +634,28 @@ function refreshKeySourceDisplay() {
 
 }
 
-// Mục XVIII — SINGLE AUTHORITY CHO AUTO TUNE. Đây là nơi DUY NHẤT thực sự gọi
-// sendKeyToAutotune(). Chỉ được gọi từ: AI cập nhật khi AI đang là nguồn active,
-// Manual SEND, Manual timeout (quay về Database/AI), Database match/miss khi Manual
-// không active. KHÔNG được gọi từ bất kỳ dropdown onchange nào (đó là preview, Mục VI).
-async function commitKeyToAutoTune(key, reason) {
+// Mục XVIII (v5.1) + Mục I/III/XI (v5.2) — SINGLE AUTHORITY CHO AUTO TUNE. Đây là nơi
+// DUY NHẤT thực sự gọi sendKeyToAutotune(). Chỉ được gọi từ: AI cập nhật khi AI đang là
+// nguồn active, Manual SEND, Manual timeout (quay về Database/AI), Database match/miss khi
+// Manual không active. KHÔNG được gọi từ bất kỳ dropdown onchange nào (đó là preview).
+//
+// `source` LUÔN là "ai" | "manual" | "database" lấy TRỰC TIẾP từ resolveActiveKey() (không
+// suy luận qua chuỗi reason — chuỗi reason chỉ để log cho dễ đọc). QUAN TRỌNG (v5.2 Mục
+// I/III/XI): "AI SEND ≠ Manual SEND". Nhánh AI_CONTROL (Core/Bridge tự gửi AI xuống Plugin)
+// CHỈ được phép bỏ qua gửi trực tiếp khi source THỰC SỰ là "ai" — tuyệt đối không áp dụng cho
+// "manual"/"database", nếu không Manual SEND/Manual Timeout sẽ bị "nuốt" im lặng khi app đang
+// ở chế độ AI_CONTROL (lỗi thật, đã phát hiện + sửa khi rà lại theo task v5.2 trước đây —
+// bản merge vào main bị thiếu fix này nên áp lại ở đây).
+async function commitKeyToAutoTune(key, source, reason) {
 
     if (key === keyState.autoTune.committedKey) return; // giá trị không đổi thật -> khỏi gửi lại, tránh spam Plugin
 
-    if (isAiControlActive()) {
+    if (source === "ai" && isAiControlActive()) {
 
         // AI_CONTROL: renderer KHÔNG tự gửi lệnh AI xuống Plugin nữa (giữ NGUYÊN hành vi cũ) —
         // Workflow -> PluginController -> Bridge lo phần gửi thật (xem onPluginCommand cuối file).
+        // CHỈ áp dụng khi nguồn thật là AI — Manual SEND/Manual Timeout(->Database)/Database vẫn
+        // LUÔN gửi trực tiếp qua sendKeyToAutotune() bên dưới, không phụ thuộc AI_CONTROL bật/tắt.
         console.log(`[ControlSource] AI_CONTROL — bỏ qua gửi Key trực tiếp (${reason}), chờ Bridge từ Core.`);
         setStatus("dot-key", "online");
         keyState.autoTune.committedKey = key;
@@ -662,11 +672,11 @@ async function commitKeyToAutoTune(key, reason) {
         setStatus("dot-key", "online");
         keyState.autoTune.committedKey = key;
         lastPluginKey = key;
-        if (keyInfoEl && reason === "Manual Override") keyInfoEl.textContent = `Manual Key (${result.driverUsed})`;
+        if (keyInfoEl && source === "manual") keyInfoEl.textContent = `Manual Key (${result.driverUsed})`;
     } else {
         setStatus("dot-key", "offline");
         console.error(`commitKeyToAutoTune(${reason}) lỗi:`, result.detail);
-        if (keyInfoEl && reason === "Manual Override") keyInfoEl.textContent = "Lỗi gửi Key";
+        if (keyInfoEl && source === "manual") keyInfoEl.textContent = "Lỗi gửi Key";
     }
 
     refreshKeySourceDisplay();
@@ -684,13 +694,18 @@ function syncActiveKey(reason, { commit = true } = {}) {
 
     originalKey = resolved.value; // mirror — nhiều chỗ code cũ (SoundShifter, ModEngine, transposeKey) vẫn đọc biến này
     if (currentKeyEl) currentKeyEl.textContent = resolved.value;
-    if (keySelector && resolved.source !== "manual") keySelector.value = resolved.value; // đồng bộ dropdown khi KHÔNG phải Manual
+
+    // MENU UI FIX (Claude 2, Mục 4/5): khi KHÔNG phải Manual, Dropdown phải hiển thị nhãn
+    // tĩnh "AI Key Detect" — TUYỆT ĐỐI không đưa tên Key thật (vd "G# Minor") vào Dropdown,
+    // vì nhìn sẽ giống như người dùng đã tự chọn Manual dù chưa hề thao tác gì. Trước đây
+    // gán nhầm keySelector.value = resolved.value (tên Key thật) — đã sửa.
+    if (keySelector && resolved.source !== "manual") keySelector.value = "AI Key Detect";
 
     refreshModPreviewIfOn(); // Mục XIV/Test 11-12: MOD phải nhìn thấy Active Key MỚI NHẤT
     logKeySource(reason);
     refreshKeySourceDisplay();
 
-    if (commit) commitKeyToAutoTune(resolved.value, reason);
+    if (commit) commitKeyToAutoTune(resolved.value, resolved.source, reason);
 
 }
 
